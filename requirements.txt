# ENXAME - Dependências base compartilhadas por juiz/, bibliotecario/, agentes/ e core/
#
# [Inferência] Este arquivo não existia no repositório, embora
# juiz/requirements.txt, bibliotecario/requirements.txt e
# agentes/requirements.txt façam `-r ../requirements.txt`. As versões abaixo
# foram inferidas a partir dos imports encontrados no código (fastapi,
# pydantic, httpx, websockets, zeroconf em core/discovery, typer/rich em
# cli/enxame.py) e de um servidor ASGI (uvicorn) necessário para de fato
# executar os apps FastAPI de juiz/app.py e bibliotecario/app.py, já que
# não encontrei nenhum uvicorn.run() nem outro comando de start no
# repositório. Confirme as versões antes de travar isso em produção.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
websockets==13.1
typer==0.12.5
rich==13.8.1
zeroconf==0.135.0
